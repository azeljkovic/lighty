{
  "name": "lighty",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/index.js",
    "dist/index.d.ts"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "npm run build",
    "run": "pnpm run build && VERBOSE_LOGGING=true node run.ts",
    "test": "pnpm run build && node --test",
    "test:verbose": "pnpm run build && VERBOSE_LOGGING=true node --test"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
